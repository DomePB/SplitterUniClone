version: "3.1"
services:
  db:
    image: postgres:15-alpine
    ports:
      - "5432:5432"
    environment:
      - "POSTGRES_USER=${USER}"
      - "POSTGRES_PASSWORD=${PASSWORD}"
      - "POSTGRES_DB=Splitter"